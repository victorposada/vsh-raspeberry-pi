---
version: '3'

services:
  influxdb:
    image: influxdb:latest
    container_name: influxdb
    ports:
      - "8086:8086"
    environment:
      - INFLUXDB_DB=influx
    volumes:
      - ./influx:/var/lib/influxd
      - ./ssl/vsh.crt:/etc/ssl/vsh.crt
      - ./ssl/vsh.key:/etc/ssl/vsh.key